@model Models.HomeNewsModel


<div class="container-fluid col-sm-3 p-4">
    <div class="row">
        @foreach (var product in Model.newslist)
        {
            <div class="col-sm-3 p-1">


                @await Html.PartialAsync("_PictureBox", product)

            </div>
        }
    </div>

</div>